#include <bits/stdc++.h>
using namespace std;
// 在字符串中找出连续最长的数字串含+-号,优先级低,跳过
int main()
{
   int number,days;
   cin>>number>>days;

   vector<int> items(number);//存储每个商品的数量
   for (int i = 0; i < count; i++)
   {
    /* code */
   }
   
}